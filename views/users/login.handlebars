<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    {{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
        {{else}}
    {{/each}}
    
    <h4>Fazer login</h4>
        <div class="card">
        <div class="card-body">
        <form action="/users/login" method="post">
            <label for="email" class="mb-1">Email</label>
            <input type="email" name="email" placeholder="Informe o seu email" class="form-control mb-4" required>
            <label for="senha" class="mb-1">Senha</label>
            <input type="password" name="senha" placeholder="Informe sua senha" class="form-control mb-4" required>
        
            <button type="submit" class="btn btn-success mt-4">Entrar</button>
            </form>
        </div>
        </div>

</body>
</html>