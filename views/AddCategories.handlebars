<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add categories</title>
</head>
<body>
    
    {{#each texto}}
    <div class="alert alert-danger">{{texto}}</div>
        {{else}}
    {{/each}}
    
    <h3>Nova categoria</h3>
        <div class="card">
        <div class="card-body">
        <form action="/categories/new" method="post">
            <label for="name" class="mb-1">Categoria</label>
                <input type="text" name="name" placeholder="Nome da categoria" class="form-control mb-2" required>
            <label for="slug" class="mb-1">Slug</label>
                <input type="text" name="slug" placeholder="slug da categoria" class="form-control mb-4" required>
            <button type="submit" id="btn-category" class="btn btn-success">Criar categoria</button>
            </form>
        </div>
        </div>

</body>
</html>